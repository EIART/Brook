{
  "name": "lyrics-probe",
  "version": "1.0.0",
  "description": "Lightweight probe that reads playback state from Spotify/NeteaseMusic and sends it over WebSocket",
  "type": "module",
  "scripts": {
    "start": "npx tsx src/index.ts",
    "test": "npx vitest run"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
